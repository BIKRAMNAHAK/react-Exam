const initialState = {
    posts: [],

};

const blogReducers = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state

    }
}
    export default blogReducers